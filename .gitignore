# Dependencies
node_modules/

# Build output is intentionally committed: GitHub Pages serves ./dist directly
# so the site works with no CI step. Uncomment to keep the repo source-only.
# dist/

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Local scratch
*.log
.env
.env.*
